# Go2 한글 조작기 — 수정 이력 메모장

사용자가 보고한 증상 → 원인 → 수정 내용을 날짜별로 쌓아 둡니다.  
이후에도 “적용해 보니 또 ○○하다”고 말씀하시면 여기에 이어서 추가합니다.

---

## 2026-08-06 — Viewer가 안 뜨고 HTML만 열림

| 항목 | 내용 |
|------|------|
| **파트** | `go2_korean_control.py`, `go2_sim_worker.py`, 실행 스크립트 |
| **상태/증상** | 브라우저 HTML은 열리는데 MuJoCo 창이 안 뜸. UI는 “평지 실행 중”처럼 보임 |
| **원인** | 1) worker `stderr=PIPE`로 로그가 막혀 worker 정지 2) Viewer 열리기 전에 ready 전송 3) macOS에서 `mjpython` 미사용 가능 4) 실패해도 성공 상태 표시 |
| **수정** | worker를 mjpython으로 기동, stderr→`go2_worker.log`, Viewer 오픈 후에만 ready, 실패 시 오류 표시, `scripts/run_lab.sh` / `check_env.sh` 추가 |

---

## 2026-08-06 — 파라미터 설명 때문에 적용 버튼을 못 누름 / 값이 초기화됨

| 항목 | 내용 |
|------|------|
| **파트** | `go2_control_panel_ko.html` (UI) |
| **상태/증상** | Forward Bias 등 설명 카드가 커지며 하단 “파라미터 적용” 버튼을 가림. 다른 곳을 클릭하면 슬라이더 값이 다시 초기화되어 적용 불가 |
| **원인** | 1) 설명이 파라미터 전체를 hover할 때 아래쪽으로 펼쳐짐 2) 400ms 상태 폴링이 서버(미적용) 값으로 로컬 슬라이더를 덮어씀 |
| **수정** | 각 파라미터 제목 **상단 우측**에 `적용` 버튼 배치. 설명은 **이름에만** 올렸을 때 **위쪽**에 뜨고, 마우스를 떼면 사라지며 클릭을 가로채지 않음(`pointer-events: none`). 사용자가 바꾼 값(`dirty`)은 적용 전까지 서버 동기화로 덮지 않음 |

---

## 2026-08-06 — 서기/왼앞발 들기에서 발을 안 들고 밀리듯 움직임, 걷기가 뒤로 스케이트

| 항목 | 내용 |
|------|------|
| **파트** | `go2_sim_worker.py` (보행/자세 제어) |
| **상태/증상** | 1) 서기·왼앞발 들기에서 발이 안 들리고 바닥을 밀듯 앞으로 나감 2) 앞으로 걷기인데 뒤로 스케이트 타듯 후진 |
| **원인** | 1) 발 들기 시 calf를 **펴는** 방향(+)으로 잘못 조작 → 발이 바닥에 눌림 2) 걷기 스탠스에서 thigh를 **감소**시켜 발을 앞으로 끌음 → 몸이 뒤로 밀림 3) 전진에 hip(좌우 외전)을 섞어 옆으로/밀리듯 동작 4) 모드 전환 시 이전 속도가 남아 미끄러짐 |
| **수정** | Go2 관례에 맞게 재작성: calf↓=발 들기, 스윙=thigh↓(발 앞), 스탠스=thigh↑(발 뒤·몸 전진). hip은 home 유지. 서기/발들기/일시정지 전환 시 베이스 속도 0으로 리셋 |

---

## (다음에 추가할 칸)

사용자가 적용 후 추가로 말씀하신 증상은 아래 형식으로 이어서 적습니다.

```text
## YYYY-MM-DD — (짧은 제목)

| 항목 | 내용 |
|------|------|
| **파트** | |
| **상태/증상** | |
| **원인** | |
| **수정** | |
```
